import React from 'react';

const About = () => {
  return (
    <div style={styles.contentWrapper}>
      about data
    </div>
  )
};

const styles = {
  contentWrapper: {
    backgroundColor: 'red'
  }
};

export default About;